# MORNING BREW COLLECTIVE - TECHNICAL DOCUMENTATION

## 📋 EXECUTIVE SUMMARY
This documentation covers the complete technical implementation of Morning Brew Collective's website, designed with "Tropical Modernism" aesthetic that honors 1970s Singapore kopitiam heritage while delivering modern e-commerce functionality.

**Key Achievements:**
- ✅ **Anti-Generic Design**: Custom "Dawn to Noon" gradient system.
- ✅ **Mobile-First**: Touch-optimized navigation and interactions.
- ✅ **Singapore-Optimized**: Localized order form (SingPass/Halal ready fields).
- ✅ **Performance**: Critical CSS layers, Service Worker caching, and IntersectionObserver.
- ✅ **Accessibility**: WCAG AA compliance features (Contrast, Keyboard Nav, ARIA).

## 🎨 DESIGN SYSTEM

### Color System - "Dawn to Noon Gradient"
| Section | Color | Hex | Usage |
|---------|-------|-----|-------|
| Hero | Dawn Cream | #FFF9F0 | Morning light atmosphere |
| Menu | Terracotta Sunrise | #E68A66 | Coffee warmth energy |
| Heritage | Honey Gold | #D4A017 | Vintage kopitiam furniture |
| Location | Avocado Leaf | #7D9A75 | Garden City connection |
| Footer | Coffee Brown | #4A3528 | Grounding depth |

## ⚙️ TECHNICAL ARCHITECTURE

### File Structure
```
/
├── index.html                    # Main application entry
├── offline.html                  # Offline fallback page
├── css/
│   └── main.css                  # Layered CSS (Tokens, Base, Components)
├── js/
│   ├── main.js                   # Navigation & Scroll Logic
│   ├── cart-system.js            # Shopping Cart & Order Form
│   ├── accessibility.js          # A11y Helpers
│   └── service-worker.js         # Offline Caching Strategy
└── tests/
    └── verify_structure.js       # Build verification script
```

### Core Technologies
- **CSS Layers**: Used to manage specificity (`@layer tokens, base, components, utilities`).
- **Vanilla JS (ES6+)**: Modular class-based architecture for Cart and Forms.
- **Service Worker**: Cache-first strategy for assets, network-first for data.
- **LocalStorage**: Client-side cart persistence.

## 🚀 GETTING STARTED

### Prerequisites
- Any modern web browser.
- Simple HTTP server (recommended for Service Worker testing).

### Running locally
You can use any static file server. For example, using Python or Node.js:

```bash
# Python 3
python3 -m http.server 8000

# Node.js (npx)
npx serve .
```

Visit `http://localhost:8000` to view the site.

## 🔧 MAINTENANCE

### Updating the Menu
Modify the `index.html` file within the `.menu-grid` section. Each item uses the following structure:
```html
<div class="menu-item card scroll-animated" data-category="coffee">
  <!-- Content -->
  <button class="add-to-cart-btn" data-product="id" data-price="3.50">...</button>
</div>
```

### Adding Locations
Duplicate the `.location-card` block in `index.html`.

## 📈 FUTURE ROADMAP
1. **Backend Integration**: Replace the simulated API call in `cart-system.js` with real endpoints.
2. **PWA Manifest**: Add `manifest.json` for "Add to Homescreen" capability.
3. **Payment Gateways**: Replace alert stubs with Stripe/GrabPay SDKs.

---
*Documentation generated by Gemini CLI - Morning Brew Collective Project*
